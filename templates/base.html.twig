<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('ico/ico32.ico') }}">
<link rel="icon" type="image/png" sizes="16x16" href="{{ asset('ico/ico16.ico') }}">
    <title>{% block title %}Mon magnifique site{% endblock %}</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/solar/bootstrap.css">

    {% block stylesheets %}{% endblock %}

    <style>
        html, body {
            height: 100%;
            margin: 0;
        }

        body {
            display: flex;
            flex-direction: column;
            background-color: white;
            color: black;
        }

        .container {
            flex: 1; /* pousse le footer en bas */
        }

        .pied {
            background-color: #000000; /* même couleur que la navbar */
            color: white;
            padding: 20px 0;
            width: 100%;
            box-sizing: border-box;
        }

        .footer-column {
            margin-bottom: 15px;
        }

        .footer-column h5 {
            margin-bottom: 10px;
        }

        .footer-column a {
            color: white;
            text-decoration: none;
        }

        .footer-column a:hover {
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .footer-column {
                flex: 1 1 100%;
                text-align: center;
            }
        }

        {% block inline_styles %}{% endblock %}
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_accueil', {'_locale': app.request.locale}) }}">Mon Site</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('app_accueil', {'_locale': app.request.locale}) }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_loisir', {'_locale': app.request.locale}) }}">Loisir</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_login', {'_locale': app.request.locale}) }}">CV</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_contact', {'_locale': app.request.locale}) }}">Me Contacter</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('app_e_portfolio', {'_locale': app.request.locale}) }}">E-Portfolio</a></li>

                {# Dropdown langue #}
                {% set currentRoute = app.request.attributes.get('_route') %}
                {% set currentParams = app.request.attributes.get('_route_params') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#">
                        {{ app.request.locale == 'fr' ? 'Français' : 'English' }}
                    </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path(currentRoute, currentParams|merge({'_locale':'fr'})) }}">🇫🇷 Français</a>
                        <a class="dropdown-item" href="{{ path(currentRoute, currentParams|merge({'_locale':'en'})) }}">🇬🇧 English</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    {% block body %}{% endblock %}
</div>


<footer class="pied d-flex flex-wrap justify-content-around align-items-start">
    <div class="footer-column">
{#    
        <h5>Contact</h5>
        <p style="margin:0;">
            Mohamed CHIBOUB<br>
            BUT Réseaux & Télécommunications<br>
            <a href="mailto:mohamed.chiboub@example.com">mohamed.chiboub@example.com</a>
        </p>
    </div>

    <div class="footer-column">
        <h5>Quick Links</h5>
        <ul style="list-style:none; padding:0; margin:0;">
            <li><a href="{{ path('app_accueil', {'_locale': app.request.locale}) }}">Accueil</a></li>
            <li><a href="{{ path('app_loisir', {'_locale': app.request.locale}) }}">Loisir</a></li>
            <li><a href="{{ path('app_login', {'_locale': app.request.locale}) }}">CV</a></li>
            <li><a href="{{ path('app_e_portfolio', {'_locale': app.request.locale}) }}">E-Portfolio</a></li>
            <li><a href="{{ path('app_contact', {'_locale': app.request.locale}) }}">Contact</a></li>
        </ul>
    </div>

    <div class="footer-column">
        <h5>Follow Me</h5>
        <p style="margin:0;">
            <a href="https://github.com/Sudo-momolefous" target="_blank">GitHub</a><br>
            <a href="https://www.instagram.com/momolefous01/" target="_blank">Instagram</a><br>
        </p>
    </div>
#}
    <div style="width:100%; text-align:center; margin-top:15px; color:white;">
        <small>&copy; 2025 - Mohamed CHIBOUB | All rights reserved.</small>
    </div>
</footer>

{% block javascript %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}

</body>
</html>
