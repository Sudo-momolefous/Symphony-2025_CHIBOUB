<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Mon magnifique site{% endblock %}</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/solar/bootstrap.css">

    {% block stylesheets %}{% endblock %}

    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: rgb(255, 255, 255); padding: 2px 6px; }
        .pied {
            background-color: black;
            color: white;
            text-align: center;
            padding: 15px 0;
            margin-top: 20px;
        }
        body {
            background-color: white;
            color: black;
        }

        {% block inline_styles %}{% endblock %}
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Mon magnifique site</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Loisir">Loisir</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Login">CV</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Mes_info">Mes info</a>
                </li>

                {% set currentPath = app.request.pathinfo %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#">
                        {{ '/en' in currentPath ? 'English' : 'Français' }}
                    </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ currentPath|replace({'/en': ''}) }}">🇫🇷 Français</a>
                        <a class="dropdown-item" href="{% if '/en' in currentPath %}{{ currentPath }}{% else %}{{ currentPath ~ '/en' }}{% endif %}">🇬🇧 English</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-4">
    {% block body %}{% endblock %}
</div>

<footer class="pied">
    <p>&copy; 2025 - MonSite. Tous droits réservés.</p>
</footer>

{% block javascript %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}
</body>
</html>
